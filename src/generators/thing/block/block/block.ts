// import { builder, thing } from "@";
// import type { DecorativeCfg } from "./decorative.types";

// export const decorative = builder({
//   build: (cfg: DecorativeCfg, g) => thing(cfg, g),
//   defaults: { group: "Decorative", categories: ["Blocks.Deco"] }
// });
